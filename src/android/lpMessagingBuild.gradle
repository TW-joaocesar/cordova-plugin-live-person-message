apply plugin: 'com.android.application'

android {
    repositories {
        flatDir {
             dirs 'lp_messaging_sdk', '../<library_dir>/lp_messaging_sdk'
        }
    }
}

dependencies {
    compile project(':lp_messaging_sdk')
}