apply plugin: 'com.android.application'

android {

	repositories {
        flatDir {
             dirs 'lp_messaging_sdk'
        }
    }

}

dependencies {
    compile(name: 'lp_messaging_sdk', ext: 'aar')
}